# ``Learning``

Изучите основы работы с массивами в Swift — от создания до продвинутых операций с типами.

## Для кого эта статья

* **👶 Абсолютные новички** — если вы только начинаете
* **🔄 Переход с других языков** — чтобы понять особенности Swift
* **📚 Систематизация знаний** — если хотите структурировать уже известное

## Что вы изучите

* 📦 Что такое массив и зачем он нужен
* 🏗️ Как создавать и изменять массивы
* ➕ Как добавлять, удалять и искать элементы
* 🎭 Как работать с массивами разных типов

---

## Что такое массив? 🧐

Представьте, что вы ведете **список покупок**:

```
Список покупок:
1. 🥛 Молоко
2. 🍞 Хлеб
3. 🧀 Сыр
4. 🍎 Яблоки
```

В Swift такой список называется **массивом**. Это просто **пронумерованный список**, где у каждого элемента есть свой порядковый номер (индекс).

### 4 главных правила массивов

#### 📋 Правило 1: Все элементы одного типа

Массив может хранить элементы только **ОДНОГО ТИПА**. Нельзя смешивать строки и числа.

✅ **Правильно:**
```swift
let shoppingList = ["🥛", "🍞", "🧀"] // Только строки
let prices = [100, 50, 300]           // Только числа
```

❌ **Неправильно:**
```swift
let mixed = ["🥛", 100, "🍞"] // Ошибка! Разные типы
```

#### 📋 Правило 2: Порядок важен

Элементы хранятся **в том порядке**, в котором вы их добавили.

```swift
let fruits = ["🍎", "🍌", "🍊"]
print(fruits[0]) // Всегда будет "🍎" (первый)
print(fruits[1]) // Всегда будет "🍌" (второй)
```

> 🔄 Это отличает массивы от **множеств (Set)**, где порядок не гарантируется.

#### 📋 Правило 3: Можно повторять

В отличие от множеств, массивы **разрешают дубликаты**.

```swift
let votes = [5, 5, 3, 5, 4, 5]
// Число 5 встречается 4 раза — это нормально!
print("Всего оценок: \(votes.count)") // 6
```

#### 📋 Правило 4: Нумерация с нуля

Первый элемент имеет **индекс 0**, второй — 1, и так далее.

```swift
let letters = ["A", "B", "C", "D"]
// Индексы:     0    1    2    3

letters[0] // "A" (первый)
letters[3] // "D" (четвертый)
// letters[4] // 💥 ОШИБКА! Индекс за пределами
```

> ⚠️ **Запомните навсегда:** первый элемент — всегда под индексом 0!

### Простая аналогия

Представьте **почтовые ячейки** в подъезде:
* Ячейки пронумерованы по порядку: 0, 1, 2, 3...
* В каждой ячейке лежит что-то одно
* Вы знаете, что в ячейке №0 всегда первый элемент
* Ячейки идут строго по порядку

Массив работает точно так же! 🎯

---

## 1. Создание массивов 🏗️

### Пустой массив

```swift
// Способ 1: Явно указываем тип
var emptyStrings: [String] = []     // Массив строк
var emptyNumbers: [Int] = []        // Массив чисел

// Способ 2: Используем синтаксис типа
var emptyStrings2 = [String]()      // Тоже массив строк
var emptyNumbers2 = [Int]()         // Тоже массив чисел

// Способ 3: Самый короткий
var emptyStrings3: [String] = []    // Просто квадратные скобки
```

### Массив с элементами

```swift
// Swift УМНЫЙ — он сам понимает тип массива
let numbers = [1, 2, 3, 4, 5]       // Swift понял: это [Int]
let names = ["Анна", "Борис"]        // Swift понял: это [String]

// Можно указать тип явно (для ясности)
let explicitNumbers: [Int] = [1, 2, 3]
```

### Важное различие: let и var

```swift
// var — массив можно менять (добавлять/удалять)
var mutableArray = [1, 2, 3]
mutableArray.append(4)  // ✅ Работает! [1, 2, 3, 4]

// let — массив НЕЛЬЗЯ менять (он постоянный)
let immutableArray = [1, 2, 3]
// immutableArray.append(4)  // ❌ Ошибка! Нельзя изменить
```

> 🔑 **Золотое правило:** 
> * `let` = "не трогать" (постоянный)
> * `var` = "можно изменять"

---

## 2. Добавление элементов ➕

```swift
var students = ["Анна", "Борис"]
print("Начало: \(students)") // ["Анна", "Борис"]

// Добавляем ОДИН элемент в КОНЕЦ
students.append("Виктор")
print("После append: \(students)") // ["Анна", "Борис", "Виктор"]

// Добавляем на конкретное МЕСТО (по индексу)
students.insert("Денис", at: 1)  // Вставить на позицию 1
print("После insert: \(students)") // ["Анна", "Денис", "Борис", "Виктор"]

// Добавляем СРАЗУ НЕСКОЛЬКО элементов
let newStudents = ["Елена", "Федор"]
students.append(contentsOf: newStudents)
print("После append(contentsOf): \(students)")
// ["Анна", "Денис", "Борис", "Виктор", "Елена", "Федор"]
```

> 📝 **Запомните:** 
> * `append` = в конец
> * `insert(at:)` = в середину
> * `append(contentsOf:)` = добавить целый массив

---

## 3. Доступ к элементам 🔍

### Прямой доступ (опасный способ)

```swift
let colors = ["🔴 Красный", "🟢 Зеленый", "🔵 Синий"]

let firstColor = colors[0]  // "🔴 Красный"
let secondColor = colors[1] // "🟢 Зеленый"
let thirdColor = colors[2]  // "🔵 Синий"

// ⚠️ ЭТО ОПАСНО!
// let fourthColor = colors[3] // 💥 ПРОГРАММА УПАДЕТ!
```

### Безопасный доступ (рекомендуемый способ)

```swift
// first и last ВСЕГДА безопасны — они возвращают Optional
if let first = colors.first {
    print("Первый цвет: \(first)")
} else {
    print("Массив пуст!")
}

if let last = colors.last {
    print("Последний цвет: \(last)")
}

// Создаем супер-безопасный способ доступа
extension Array {
    /// Безопасно возвращает элемент по индексу или nil
    subscript(safe index: Index) -> Element? {
        return indices.contains(index) ? self[index] : nil
    }
}

// Теперь можно безопасно обращаться к любому индексу!
let safeColor = colors[safe: 10] // nil (не упадет!)
if let color = safeColor {
    print(color)
} else {
    print("Такого индекса нет, но программа жива! 🎉")
}
```

> 🛡️ **Правило безопасности:** 
> Всегда проверяйте, существует ли индекс, или используйте `.first`/`.last`

---

## 4. Изменение элементов ✏️

```swift
var grades = [4, 5, 3, 5]
print("Было: \(grades)") // [4, 5, 3, 5]

// Меняем конкретный элемент
grades[2] = 4  // Заменяем тройку на четверку
print("Стало: \(grades)") // [4, 5, 4, 5]

// Можно менять сразу несколько
grades[0...1] = [5, 5]  // Первые два элемента становятся пятерками
print("Итог: \(grades)") // [5, 5, 4, 5]
```

> ⚠️ **Важно:** При изменении убедитесь, что индекс существует!

---

## 5. Удаление элементов 🗑️

```swift
var playlist = ["Песня 1", "Песня 2", "Песня 3", "Песня 4"]
print("Плейлист: \(playlist)")

// Удаляем по индексу
let removedSong = playlist.remove(at: 1) // "Песня 2"
print("Удалили: \(removedSong)")
print("Осталось: \(playlist)") // ["Песня 1", "Песня 3", "Песня 4"]

// Удаляем первый и последний
playlist.removeFirst() // Удаляем "Песня 1"
playlist.removeLast()  // Удаляем "Песня 4"
print("После удаления краев: \(playlist)") // ["Песня 3"]

// Удаляем ВСЁ!
playlist.removeAll()
print("Пусто? \(playlist.isEmpty)") // true
```

---

## 6. Поиск элементов 🔎

```swift
let fruits = ["🍎 яблоко", "🍌 банан", "🍊 апельсин", "🍎 яблоко"]

// Проверяем, есть ли элемент
let hasApple = fruits.contains("🍎 яблоко")      // true
let hasGrape = fruits.contains("🍇 виноград")    // false

// Ищем ИНДЕКС элемента
if let index = fruits.firstIndex(of: "🍌 банан") {
    print("Банан найден на позиции \(index)") // 1
}

// Ищем ПЕРВЫЙ элемент, подходящий под условие
let numbers = [10, 23, 45, 67, 89]

// Найти первое четное число
if let firstEven = numbers.first(where: { $0 % 2 == 0 }) {
    print("Первое четное: \(firstEven)") // 10
}

// Найти индекс первого числа больше 50
if let index = numbers.firstIndex(where: { $0 > 50 }) {
    print("Первое число >50 на индексе \(index)") // 3 (число 67)
}
```

> 🧠 **Объяснение `{ $0 % 2 == 0 }`:** 
> Это замыкание — короткая запись правила поиска. `$0` означает "каждый элемент по очереди". Мы проверяем: "остаток от деления на 2 равен 0?" — значит число четное.

---

## 7. Сортировка 🔤

```swift
var unsorted = [3, 1, 4, 2]
print("Было: \(unsorted)")

// СОРТИРУЕМ НА МЕСТЕ (меняем оригинал)
unsorted.sort()
print("Стало (оригинал изменился): \(unsorted)") // [1, 2, 3, 4]

// СОРТИРУЕМ С КОПИРОВАНИЕМ (оригинал не трогаем)
let original = [3, 1, 4, 2]
let sorted = original.sorted()
print("Оригинал: \(original)") // [3, 1, 4, 2] (не изменился!)
print("Копия: \(sorted)")      // [1, 2, 3, 4]

// Сортируем по своему правилу
let words = ["яблоко", "банан", "киви", "апельсин"]

// По длине строки (от коротких к длинным)
let byLength = words.sorted { $0.count < $1.count }
print("По длине: \(byLength)") // ["киви", "яблоко", "банан", "апельсин"]

// По алфавиту (по умолчанию)
let alphabetically = words.sorted()
print("По алфавиту: \(alphabetically)") // ["апельсин", "банан", "киви", "яблоко"]
```

> 🔑 **Запомните:** 
> * `sort()` = меняет оригинал
> * `sorted()` = создает копию

---

## 8. Полезные свойства 📊

```swift
let array = [1, 2, 3, 4, 5]

array.isEmpty  // false (не пустой)
array.count    // 5 (количество элементов)
array.first    // Optional(1) (первый)
array.last     // Optional(5) (последний)

if !array.isEmpty {
    print("В массиве \(array.count) элементов")
    print("Первый: \(array.first!)") // ! — только если уверены, что не пусто
}
```

---

## 9. Продвинутая тема: Массивы с разными типами 🎭

### Проблема

Представьте, что мы пишем приложение для школы. У нас есть:

```swift
struct Student {
    let name: String
    let scores: [Int]      // Оценки ученика
}

struct Teacher {
    let name: String
    let subject: String    // Предмет, который ведет
    let salary: Int        // Зарплата
}

struct Director {
    let name: String
    let experience: Int    // Лет опыта
}
```

**Как хранить их ВСЕХ в одном списке**, если они разных типов?

### Решение: Протокол (Protocol)

**Протокол** — это как **контракт** или **анкета**. Если структура подписывает протокол, она ОБЯЗАНА выполнить его требования.

```swift
/// Протокол, объединяющий всех людей в школе
protocol Person {
    var name: String { get }           // Имя есть у всех (только чтение)
    var surname: String { get set }    // Фамилия (можно читать и изменять)
}
```

> 📖 **Расшифровка:** 
> * `{ get }` — свойство можно только читать (подходит `let`)
> * `{ get set }` — свойство можно и читать, и изменять (нужно `var`)

### Подписываем структуры под протокол

```swift
struct Student: Person {
    let name: String
    var surname: String
    let scores: [Int]
}

struct Teacher: Person {
    let name: String
    var surname: String
    let subject: String
    let salary: Int
}

struct Director: Person {
    let name: String
    var surname: String
    let experience: Int
}
```

### Создаем общий массив

```swift
// Тип массива — [Person], но внутри могут быть любые Person
let schoolMembers: [Person] = [
    Student(name: "Петр", surname: "Петров", scores: [5, 4, 5]),
    Teacher(name: "Мария", surname: "Иванова", subject: "Математика", salary: 50000),
    Student(name: "Анна", surname: "Сидорова", scores: [4, 4, 5]),
    Director(name: "Елена", surname: "Васильева", experience: 15)
]

print("В школе \(schoolMembers.count) человек")
```

### Как работать с таким массивом

```swift
// Проходим по всем
for person in schoolMembers {
    // У всех есть имя и фамилия (из протокола)
    print("👤 \(person.name) \(person.surname)")
    
    // Чтобы получить доступ к УНИКАЛЬНЫМ свойствам,
    // нужно "привести" к конкретному типу
    if let student = person as? Student {
        print("  📚 Ученик, оценки: \(student.scores)")
    } else if let teacher = person as? Teacher {
        print("  🍎 Учитель, предмет: \(teacher.subject)")
    } else if let director = person as? Director {
        print("  👔 Директор, опыт: \(director.experience) лет")
    }
}
```

> 🎯 **Аналогия:** 
> Представьте коробку с фруктами. Все они — фрукты (протокол), но яблоко (Student) можно съесть, а банан (Teacher) нужно почистить. 
> `as?` помогает понять, какой фрукт перед вами.

### Поиск и изменение

```swift
// Найдем директора с опытом 15 лет
if let directorIndex = schoolMembers.firstIndex(where: { person in
    // Проверяем, является ли человек директором И имеет ли опыт 15 лет
    (person as? Director)?.experience == 15
}) {
    print("Нашли директора на индексе \(directorIndex)")
    
    // Меняем данные (нужен var массив)
    if var director = schoolMembers[directorIndex] as? Director {
        director.experience = 16
        // schoolMembers[directorIndex] = director // Обновляем в массиве
        print("Опыт увеличен до \(director.experience)")
    }
}
```

---

## Проверь себя! ✍️

### Вопрос 1
**Какой индекс у первого элемента массива?**

- [ ] 1
- [x] 0
- [ ] Первый элемент не имеет индекса

### Вопрос 2
**Что произойдет при вызове `array[10]`, если в массиве 5 элементов?**

- [ ] Вернется nil
- [x] Программа упадет с ошибкой
- [ ] Вернется последний элемент

### Вопрос 3
**В чем разница между `sort()` и `sorted()`?**

- [x] `sort()` меняет массив, `sorted()` создает новый
- [ ] Это одно и то же
- [ ] `sort()` для чисел, `sorted()` для строк

### Вопрос 4
**Можно ли хранить в одном массиве `Int` и `String`?**

- [ ] Да, через запятую
- [x] Нет, массивы одного типа
- [ ] Можно, если обернуть в протокол

---

## Шпаргалка 📝

| Действие | Код | Пояснение |
|----------|-----|-----------|
| **Создать пустой** | `var array = [Int]()` | Пустой массив чисел |
| **Создать с данными** | `let array = [1, 2, 3]` | Массив из трех чисел |
| **Добавить в конец** | `array.append(4)` | Добавляет 4 в конец |
| **Вставить** | `array.insert(0, at: 0)` | Вставляет 0 в начало |
| **Получить элемент** | `let item = array[2]` | Берет элемент с индексом 2 |
| **Удалить** | `array.remove(at: 1)` | Удаляет элемент с индексом 1 |
| **Проверить наличие** | `array.contains(5)` | true, если есть 5 |
| **Найти индекс** | `array.firstIndex(of: 3)` | Индекс числа 3 |
| **Отсортировать** | `array.sorted()` | Возвращает сортированную копию |
| **Количество** | `array.count` | Сколько элементов |
| **Проверить пустоту** | `array.isEmpty` | true, если пусто |

---

## Частые ошибки новичков ⚠️

### Ошибка 1: Забывают про индекс 0
```swift
let array = [10, 20, 30]
let first = array[1] // ❌ Думают, что это первый, а это ВТОРОЙ!
let correct = array[0] // ✅ Правильно: первый элемент
```

### Ошибка 2: Не проверяют границы
```swift
let array = [1, 2, 3]
let element = array[5] // 💥 Программа упадет!
// ✅ Надо проверять:
if array.count > 5 {
    let element = array[5]
}
```

### Ошибка 3: Пытаются изменить let-массив
```swift
let array = [1, 2, 3]
array.append(4) // ❌ Ошибка! let = неизменяемый
// ✅ Надо использовать var:
var mutableArray = [1, 2, 3]
mutableArray.append(4) // ✅ Работает!
```

---

## Что дальше? 🚀

Поздравляю! Вы освоили массивы — фундаментальную структуру данных. Теперь вы готовы к изучению:

| Тема | Описание |
|------|----------|
| **Множества (Sets)** | Как массивы, но без порядка и без повторов |
| **Словари (Dictionaries)** | Хранят пары "ключ-значение" |
| **Опционалы (Optionals)** | Безопасная работа с nil |

## Полезные ссылки

* [Официальная документация Apple](https://developer.apple.com/documentation/swift/array)
* [The Swift Programming Language (на русском)](https://swiftbook.ru/content/languageguide/collection-types/)
* [Swift.org: Arrays](https://docs.swift.org/swift-book/LanguageGuide/CollectionTypes.html)

---

> 🎉 **Готово!** Вы только что изучили массивы в Swift. 
> Практикуйтесь, экспериментируйте, и скоро это станет вашей второй натурой!
