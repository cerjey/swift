//
//  Set.swift
//  Learning
//
//  Created by sedesyatskiy on 12.11.2025.
//

import Foundation

// MARK: - 📚 Что такое Set (Множество)?

/// ## 🤔 Простыми словами
/// Представь себе **мешок**, в который можно кидать разные предметы.
///
/// **Главные правила этого мешка:**
/// 1.  🏷️ **Все предметы одного типа.** Ты не сможешь смешать яблоки 🍎 и номера телефонов 📞 в одном мешке.
/// 2.  🔀 **Порядок не важен.** Когда ты захочешь достать предметы из мешка, они вывалятся в случайном порядке, а не в том, в котором ты их кидал.
/// 3.  🚫 **Никаких повторов.** Если ты кинешь в мешок 5 одинаковых зеленых яблок 🍏, то, когда ты захочешь их пересчитать, увидишь только одно. Мешок автоматически удаляет дубликаты.
///
/// ## ⚖️ Сравнение с Массивом (Array)
/// *   **Массив (Array)** — это как пронумерованная полка 📚. У каждой книги есть свой четкий индекс (номер полки), и ты всегда знаешь, где какая книга лежит. Книги могут повторяться (хоть 10 одинаковых).
/// *   **Множество (Set)** — это как разобранная куча в гараже 🧰. Ты просто складываешь инструменты в ящик. Тебе не важно, какой лежит сверху, главное — что он там есть и он в единственном экземпляре.
///
/// ## 👍 Когда использовать Set
/// *   Когда тебе нужно хранить только **уникальные** значения (например, список логинов пользователей).
/// *   Когда тебе нужно **быстро** проверить, есть ли элемент в коллекции (это работает быстрее, чем в массиве).
/// *   Когда порядок элементов для тебя не важен.
///
/// - Important: Внимание! В сете **нет индексов**. Нельзя обратиться к элементу по типу `mySet[2]`, потому что элементы лежат в случайном порядке.

// MARK: - 🏗️ Объявление и инициализация

/// Пример: Создание массива (для сравнения)
var arrayOfNames: [String] = ["Алиса", "Борис", "Алиса"] // Содержит: "Алиса", "Борис", "Алиса" (дубликат есть)

/// Пример: Создание множества (Set) с именами
/// Обрати внимание на синтаксис `Set<String>` или `Set([...])`
var setOfNames: Set<String> = ["Алиса", "Борис", "Алиса"]
print(setOfNames) // Выведет: ["Борис", "Алиса"] (порядок случаен, дубликат удален)

// MARK: - ✍️ Работа с элементами (CRUD)

// MARK: ➕ Добавление (Insert)

/// `insert(_:)` - Добавляет элемент в сет.
/// Так как индексов нет, элемент просто "падает" в общую кучу.
setOfNames.insert("Виктор")
print(setOfNames) // Выведет, например: ["Борис", "Виктор", "Алиса"]

/// Если попытаться добавить существующий элемент, ничего не произойдет.
setOfNames.insert("Алиса") // Алиса уже есть в сете
print(setOfNames) // Состав не изменится: ["Борис", "Виктор", "Алиса"]

// MARK: ➕ Добавление сложных типов (Hashable)

/// Чтобы добавить свою структуру или класс в Set, они должны соответствовать протоколу `Hashable`.
/// Swift автоматически добавляет `Hashable` для структур, если все их свойства `Hashable`.
struct Car: Hashable {
    let brand: String
    let model: String
    let year: Int
}

var uniqueCars: Set<Car> = []

let car1 = Car(brand: "BMW", model: "X5", year: 2020)
let car2 = Car(brand: "Audi", model: "Q7", year: 2021)
let car3 = Car(brand: "BMW", model: "X5", year: 2020) // Полностью идентична car1

uniqueCars.insert(car1)
uniqueCars.insert(car2)
uniqueCars.insert(car3) // Эта машина не добавится, так как car1 уже есть

print(uniqueCars.count) // Выведет: 2 (car1 и car2)

// MARK: ❌ Удаление (Remove)

/// `remove(_:)` - Удаляет конкретный элемент.
setOfNames.remove("Борис")
print(setOfNames) // Выведет: ["Виктор", "Алиса"]

/// `removeAll()` - Очищает все множество.
setOfNames.removeAll()
print(setOfNames) // Выведет: []

// MARK: 🔍 Проверка наличия (Contains)

/// `contains(_:)` - Проверяет, есть ли элемент в сете. Работает очень быстро!
if setOfNames.contains("Алиса") {
    print("Алиса в множестве есть! 🎉")
} else {
    print("Алисы нет 😢")
}

// MARK: 🔄 Итерация (Перебор)

/// Так как порядок не гарантирован, элементы могут выводиться в разной последовательности каждый раз.
let fruits: Set = ["🍎", "🍌", "🍒", "🍊"]
for fruit in fruits {
    print("Фрукт: \(fruit)")
}
// Выведет (порядок может отличаться):
// Фрукт: 🍌
// Фрукт: 🍎
// Фрукт: 🍊
// Фрукт: 🍒

// MARK: - 🧰 Полезные операции (Теория множеств)

/// Множества (Set) поддерживают математические операции, знакомые из школьной программы.
/// Это их главная "фишка", за которую их и любят.

let swiftDevelopers: Set = ["Анна", "Иван", "Ольга"]
let kotlinDevelopers: Set = ["Иван", "Петр", "Елена"]

// MARK: ⚔️ Пересечение (Intersection)
/// Элементы, которые есть **и там, и там**.
let bothLanguageDevelopers = swiftDevelopers.intersection(kotlinDevelopers)
print("Знают оба языка: \(bothLanguageDevelopers)") // Выведет: ["Иван"]

// MARK: ➕ Объединение (Union)
/// Все элементы из обоих множеств, без повторов.
let allDevelopers = swiftDevelopers.union(kotlinDevelopers)
print("Все разработчики: \(allDevelopers)") // Выведет: ["Анна", "Иван", "Ольга", "Петр", "Елена"]

// MARK: ➖ Разность (Subtracting)
/// Элементы из первого множества, которых **нет** во втором.
let onlySwift = swiftDevelopers.subtracting(kotlinDevelopers)
print("Знают только Swift: \(onlySwift)") // Выведет: ["Анна", "Ольга"]

// MARK: ✅ Симметричная разность (SymmetricDifference)
/// Элементы, которые есть только в одном из множеств (исключая пересечение).
let exclusiveDevelopers = swiftDevelopers.symmetricDifference(kotlinDevelopers)
print("Знают только один язык: \(exclusiveDevelopers)") // Выведет: ["Анна", "Ольга", "Петр", "Елена"]

// MARK: - 🔗 Отношения между множествами

let group1: Set = [1, 2, 3]
let group2: Set = [1, 2, 3, 4, 5]
let group3: Set = [6, 7]

/// Является ли множество подмножеством другого?
/// (Все элементы group1 есть в group2?)
print(group1.isSubset(of: group2)) // true

/// Является ли множество надмножеством другого?
/// (Содержит ли group2 все элементы group1?)
print(group2.isSuperset(of: group1)) // true

/// Не пересекаются ли множества?
/// (Нет ли общих элементов у group1 и group3?)
print(group1.isDisjoint(with: group3)) // true (общих элементов нет)

// MARK: - 🎯 Итоги

/// ## Когда что использовать?
///
/// 1.  **Нужен порядок и индексы?** Используй **Array** (Массив). 📚
/// 2.  **Нужны только уникальные значения и быстрый поиск?** Используй **Set** (Множество). 🧰
/// 3.  **Нужны уникальные ключи и значения?** Используй **Dictionary** (Словарь). 📖 (мы изучим позже)
